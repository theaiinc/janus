server:
  address: 0.0.0.0:8088

registry:
  path: /var/lib/janus/janus.registry.json
  refreshInterval: 30s
  timeout: 5s

dataPlane:
  mode: direct

auth:
  storePath: /var/lib/janus/janus.auth.json

onboarding:
  enabled: true
  tenant: default
  pairingTTL: 10m
