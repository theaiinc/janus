# Janus npm distributions

This directory contains two independently published packages:

- `janus/`: `@theaiinc/janus`, the platform binary CLI wrapper.
- `janus-client/`: `@theaiinc/janus-client`, the JavaScript emitter and receiver SDK.
