services:
  janus:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8088:8088"
    volumes:
      - janus-cloudflare-data:/var/lib/janus

volumes:
  janus-cloudflare-data:
